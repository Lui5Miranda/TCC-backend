services:
  - type: web
    name: tcc-backend
    env: python
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: python src/server.py
    envVars:
      - key: PORT
        value: 5000
      - key: FLASK_ENV
        value: production
      - key: FLASK_DEBUG
        value: false
