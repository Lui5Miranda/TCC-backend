# Configuração do Sistema de Correção de Gabaritos
# Copie este arquivo para .env e ajuste os valores conforme necessário

# ============================================================================
# AMBIENTE
# ============================================================================
# Ambiente de execução: development, production, testing
FLASK_ENV=production

# Ativar modo debug (NUNCA em produção!)
FLASK_DEBUG=false

# ============================================================================
# SERVIDOR
# ============================================================================
# Host do servidor (0.0.0.0 para aceitar conexões externas)
HOST=0.0.0.0

# Porta do servidor (Render define automaticamente via variável PORT)
# PORT=5000

# ============================================================================
# CORS (Cross-Origin Resource Sharing)
# ============================================================================
# Origens permitidas (separadas por vírgula)

# Development:
# CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Production - IMPORTANTE: Substitua pelo seu domínio do frontend!
# Exemplo: Se seu frontend está em https://seu-app.vercel.app
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ============================================================================
# SEGURANÇA
# ============================================================================
# Tamanho máximo de upload em bytes (64MB = 67108864)
MAX_CONTENT_LENGTH=67108864

# Tipos de imagem permitidos (separados por vírgula)
ALLOWED_EXTENSIONS=png,jpg,jpeg,gif,bmp,tiff

# ============================================================================
# LOGGING
# ============================================================================
# Nível de log: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Formato do log
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# ============================================================================
# PROCESSAMENTO DE IMAGEM
# ============================================================================
# Dimensões mínimas da imagem (largura,altura)
MIN_IMAGE_WIDTH=100
MIN_IMAGE_HEIGHT=100

# Dimensões máximas da imagem (largura,altura)
MAX_IMAGE_WIDTH=8000
MAX_IMAGE_HEIGHT=8000

# Tamanho mínimo de bolha em pixels
MIN_BUBBLE_SIZE=18

# Threshold de confiança para marcação válida
CONFIDENCE_THRESHOLD=1.5

# ============================================================================
# CACHE
# ============================================================================
# Tamanho máximo do cache (número de itens)
CACHE_MAX_SIZE=50

# Tempo de vida do cache em segundos (1800 = 30 minutos)
CACHE_TTL_SECONDS=1800

# ============================================================================
# DESENVOLVIMENTO
# ============================================================================
# Diretório de uploads
UPLOAD_FOLDER=uploads

# ============================================================================
# PRODUÇÃO (Render)
# ============================================================================
# Secret key para sessões (gere uma chave segura em produção)
# SECRET_KEY=sua-chave-secreta-aleatória-aqui

# Database URL (se aplicável no futuro)
# DATABASE_URL=postgresql://user:pass@localhost/dbname

# ============================================================================
# MONITORAMENTO (opcional)
# ============================================================================
# Sentry DSN para tracking de erros
# SENTRY_DSN=https://...@sentry.io/...

# ============================================================================
# NOTAS IMPORTANTES PARA DEPLOY NO RENDER
# ============================================================================
# 1. NUNCA commite o arquivo .env no git
# 2. Configure as variáveis de ambiente no painel do Render
# 3. A variável PORT é definida automaticamente pelo Render
# 4. CORS_ORIGINS deve incluir o domínio do seu frontend
# 5. FLASK_DEBUG deve ser sempre false em produção
# 6. Use HTTPS sempre em produção
# 7. Monitore os logs regularmente
